[package]
name = "health"
edition = "2024.beta"

[dependencies]
suilend = { local = "../../../contracts/suilend" }
# Cannot import this because conflicting summaries
#obligation_spec = { local = "../obligation_spec" }
dummy_pool = { local = "../dummy_pool"}
cvlm = { git = "https://github.com/Certora/cvl-move-proto.git", subdir = "cvlm", rev = "main" }
certora_sui_summaries = { git = "https://github.com/Certora/cvl-move-proto.git", subdir = "certora_sui_summaries",  rev = "main" }

[addresses]
health = "0x0"